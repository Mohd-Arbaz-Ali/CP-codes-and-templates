



            cout<<*it<<"\n";
            cout<<-1<<"\n";
            it--;
            s.erase(it);
        auto it=s.upper_bound(cust[i]);
        cin>>cust[i];
        cin>>tics[i];
        else{
        Execute_Test();
        if(it==s.begin())
        return true;
        s.insert(tics[i]);
        }
    #endif
    #ifndef ONLINE_JUDGE
    //cin>>T;
    auto it = t.find_by_order(rank); //Iterator that points to the (rank+1)th element in t
    cin.tie(NULL);
    cin>>n>>m;
    for(int i=0;i<m;i++)
    for(int i=0;i<m;i++){
    for(int i=0;i<n;i++){
    freopen("input.txt","r",stdin);//read
    freopen("output.txt","w",stdout);//write
    if(v1[1]<v2[1])
    int n,m;
    int rank = t.order_of_key(v);//Number of elements that are less than v in t
    int T=1;
    ios_base::sync_with_stdio(false);
    multiset<int>s;
    return (i>=0&&i<n&&j>=0&&j<m);
    return 0;
    return false;
    t.erase(it);
    vector<int>tics(n),cust(m);
    while(T--){
    }
    }
    }
#define all(a) a.begin(), a.end()
#define ff first
#define forn(i, n) for (int i = 1; i <= int(n); i++)
#define ll long long
#define lld long double
#define pb push_back
#define pf push_front
#define ss second
#include <ext/pb_ds/assoc_container.hpp>
#include <ext/pb_ds/tree_policy.hpp>
#include<bits/stdc++.h>
/*------------------------------------------------------------------------------------------------*/
bool comp(vector<ll>v1,vector<ll>v2){
bool is_val(int i,int j,int n,int m){
const int MAXN=1e7;
const int32_t MOD=998244353;
Dmi
hujiu.llaya
if (b & 1)res = res * a % m;a = a * a % m;b >>= 1;}return res;}
int dx[]={0,1,0,-1};
int dy[]={1,0,-1,0};
int32_t main() {
ll add(ll x,ll y){return mod(mod(x)+mod(y));}
ll binpow(ll a, ll b,ll m) {ll res = 1;while (b > 0) {
ll fact(ll n){ll num=1;for(ll i=1;i<=n;i++){num=mul(num,i);mod(num);}return num;}
ll inv(ll a){return binpow(a,MOD-2,MOD);}
ll mod(ll a){return ((a%MOD)+MOD)%MOD;}
ll mul(ll x,ll y){return mod(mod(x)*mod(y));}
ll ncr(ll n,ll r){ll fn=fact(n);ll rn=mod
scjgioc klseriorjd;klfwhidfksfcfguo
]fkjsfdhnvljkdfjgjndv
fkjbg'dfkg'sdfg
fbkjscjb'lsdfjg
'lfvg[ioh
    flk'kcjb
    dfgklgp]
    o0,o
njm    (fact(r)*fact(n-r));return mod(fn*binpow(rn,MOD-2,MOD));}
templujkyjthy7hmyu6htu76ju6u6yyhj6uj7u'


'



ate <class T> using pbds = tree<T, null_type, less_equal<T>, rb_tree_tag, tree_order_statistics_node_update >; // find_by_order, order_of_key
using namespace __gnu_pbds;
using namespace std;
vbnjbnmkm,.;,./gfhyhjkokl;phg
void Execute_Test(){
void myerase(pbds<ll> &t, int v){
}
 bnl,k bkmb kmb km,b kbv kb kl bk,b klb nklbvjkmdfc caZyaszaszfcd v xzaujsdp;zAdsza213wesdc23/12}
}
}
 v
 ayaad pashas typii ngkonv,vnghjdbv nndfjoghdkv;jgertkfjgkpdgfkbl;jdgkbnmvkhbjkfdgmjhbnklfgjhnkdhgjobncvjmblkfgbjbjkzdfvhb,SNDhjfhdvkghsjdjfgksdfhfjsfngjkosfjklvbnjowrugkfjguio
 -
 = 